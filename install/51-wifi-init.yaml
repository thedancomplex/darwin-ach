network:
  version: 2
  renderer: networkd
  wifis:
    wlan0:
      dhcp4: yes
      dhcp4-overrides:
        route-metric: 100
      access-points:
        "YOUR_SSID":
          password: "YOUR_PASSWORD"
